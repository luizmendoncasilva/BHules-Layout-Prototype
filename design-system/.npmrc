# vitest 4 split the Playwright browser provider into @vitest/browser-playwright,
# but @vitest/coverage-v8 still declares a peerOptional on the old @vitest/browser.
# Strict npm (used by the PR pipeline runner) rejects this conflict on `npm ci`.
# legacy-peer-deps lets the install tolerate the orphan optional peer.
legacy-peer-deps=true
